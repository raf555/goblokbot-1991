const { getContentFromEvent } = require("@bot/utility");

module.exports = {
  execImage: executeImage
};

async function executeImage(event) {
  // to be added more later if I wanted to

  return null;
}
